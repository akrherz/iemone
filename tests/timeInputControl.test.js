import { setupTimeInputControl } from '../src/timeInputControl';

describe('TimeInputControl', () => {
    it('should import setupTimeInputControl function', () => {
        expect(typeof setupTimeInputControl).toBe('function');
    });
});
