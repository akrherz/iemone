import { setupLayerControls } from '../src/layerControls';

describe('LayerControls', () => {
    it('should import setupLayerControls function', () => {
        expect(typeof setupLayerControls).toBe('function');
    });
});
